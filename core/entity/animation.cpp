#include "include/entity/animation.h"

namespace core
{
    uint32_t Animation::s_switchTime = 0.5f;

    Animation::Animation(sf::Texture& texture, sf::Vector2f pos, uint32_t length)
    {
        
    }

    sf::Sprite Animation::createSprite(sf::Texture& texture, sf::Vector2f pos)
    {
        
    }

    void Animation::update()
    {

    }

    void Animation::draw(sf::RenderWindow& window)
    {

    }
}